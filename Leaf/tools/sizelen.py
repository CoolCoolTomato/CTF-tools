class Sizelen:

    def big(self, text):
        text = str(text)
        return text.upper()

    def small(self, text):
        text = str(text)
        return text.lower()

    def lens(self, text):
        text = str(text)
        return str(len(text))

